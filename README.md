- Antes de iniciar o teste, deve-se setar o trimestre de referência dos dados que compõe a leva de arquivos. O método que
que realiza a configuração está em base/BaseTest.java. Basta setar em 'primeiro', 'segundo', 'terceiro' ou 'quarto', de
acordo com o trimestre que está sendo validado.

- Para gerar o relatório dos testes, executar o comando 'allure serve' no prompt.